@using Dima.Web.Components.Identity
@inherits LayoutComponentBase


<MudThemeProvider
    @ref="_mudThemeProvider"
    Theme="Configuration.Theme"
    @bind-IsDarkMode="_isDarkMode"/>

<MudDialogProvider />

<MudSnackbarProvider/>

<MudPopoverProvider/>

<AuthorizeView>
    <Authorized>
        <MudLayout>
            <MudAppBar Elevation="0">
                
                <MudIconButton 
                    Icon="@(_isDrawerOpen ? Icons.Material.Rounded.Menu : Icons.Material.Rounded.MenuOpen)" 
                    Color="Color.Inherit"
                    Edge="Edge.Start"
                    OnClick="ToggleDrawer"></MudIconButton>
                
                <MudText Typo="Typo.h6">Dima - Controle Financeiro</MudText>
                
                <MudSpacer/>
                <MudText Typo="Typo.body1">@context.User.Identity?.Name</MudText>
                
                <MudIconButton 
                    Class="ma-4"
                    Color="Color.Inherit"
                    Icon="@(_isDarkMode ? Icons.Material.Filled.LightMode : Icons.Material.Filled.DarkMode)"
                    OnClick="ToggleDarkMode"
                    />

            </MudAppBar>
            
            <MudDrawer @bind-Open="@_isDrawerOpen">
                <NavMenu />
            </MudDrawer>
            
            <MudMainContent>
                <MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="mt-8 mb-8">
                    @Body
                </MudContainer>
            </MudMainContent>
        </MudLayout>
    </Authorized>
    <NotAuthorized>
        <LoginRedirect />
    </NotAuthorized>
    
</AuthorizeView>

@code{

    private bool _isDarkMode = false;
    private bool _isDrawerOpen = true;

    private MudThemeProvider _mudThemeProvider = null!;

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            // _isDarkMode = await _mudThemeProvider.GetSystemDarkModeAsync();
            // await _mudThemeProvider.WatchSystemDarkModeAsync(OnSystemPreferenceChaged);
            // StateHasChanged();
        }
    }

    private Task OnSystemPreferenceChaged(bool newValue)
    {
        _isDarkMode = newValue;
        StateHasChanged();

        return Task.CompletedTask;
    }

    private void ToggleDrawer() => _isDrawerOpen = !_isDrawerOpen;
    
    private void ToggleDarkMode() => _isDarkMode = !_isDarkMode;
    
}
