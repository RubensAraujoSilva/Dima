<div class="sidebar-container d-flex flex-column py-6 px-3">
    
    <div class="logo-section d-flex align-center justify-center mb-8 px-4">
        <div class="logo-wrapper">
            <img src="images/logo.png" alt="Dima" class="logo-img" />
        </div>
    </div>

    <MudNavMenu Rounded="true" Margin="Margin.Dense" Class="custom-nav-menu">
        
        <div class="nav-group-label mt-4">GESTÃO</div>
        
        <MudNavLink Href="/" Match="NavLinkMatch.All" Icon="@Icons.Material.Outlined.AutoGraph">
            <div class="d-flex align-center justify-space-between flex-grow-1">
                Dashboard
            </div>
        </MudNavLink>
        
        <MudNavLink Href="/lancamentos/historico" Icon="@Icons.Material.Outlined.ReceiptLong">
            Transações
        </MudNavLink>

        <MudNavLink Href="/categorias" Icon="@Icons.Material.Outlined.Tune">
            Categorias
        </MudNavLink>

        <div class="nav-group-label mt-8">CONFIGURAÇÕES</div>

        <MudNavLink Href="/premium" Icon="@Icons.Material.Outlined.AutoAwesome" Class="premium-link">
            <div class="d-flex align-center">
                Dima Premium
            </div>
        </MudNavLink>
        
        <MudNavLink Href="/sair" Icon="@Icons.Material.Outlined.PowerSettingsNew" Class="exit-link">
            Sair
        </MudNavLink>

    </MudNavMenu>

    
</div>

<style>
    /* 1. Tipografia e Cores das Labels */
    .nav-group-label {
        font-size: 0.65rem;
        font-weight: 800;
        letter-spacing: 0.1rem;
        color: var(--mud-palette-text-secondary);
        opacity: 0.4;
        padding-left: 16px;
        margin-bottom: 8px;
        text-transform: uppercase;
    }

    /* 2. Customização dos NavLinks */
    .custom-nav-menu .mud-nav-link {
        margin: 2px 0;
        padding: 10px 8px !important;
        border-radius: 10px !important;
        color: var(--mud-palette-text-secondary) !important;
        transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    }

    /* 3. Efeito Ativo (Elevado) */
    .custom-nav-menu .mud-nav-link.active {
        background: rgba(var(--mud-palette-primary-rgb), 0.08) !important;
        color: var(--mud-palette-primary) !important;
        font-weight: 700;
    }

    .custom-nav-menu .mud-nav-link.active .mud-nav-link-icon {
        color: var(--mud-palette-primary) !important;
    }

    /* 4. Item Premium Especial */
    .premium-link {
        background: linear-gradient(90deg, rgba(201, 170, 255, 0.1) 0%, rgba(255, 126, 179, 0.05) 100%);
        border: 1px solid rgba(201, 170, 255, 0.2) !important;
        margin-top: 4px !important;
    }

    .premium-link:hover {
        border-color: var(--mud-palette-primary) !important;
    }

    /* 5. Card de Usuário Estilo ERP */
    .user-card {
        background: rgba(0,0,0,0.03);
        border-radius: 12px;
        border: 1px solid rgba(255,255,255,0.05);
    }

    .logo-img {
        width: 120px;
        transition: transform 0.3s ease;
    }

    .logo-img:hover {
        transform: scale(1.05);
    }

    /* 6. Ajuste para o Dark Mode */
    .mud-theme-dark .user-card {
        background: rgba(255,255,255,0.03);
    }
</style>